# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Локальные подключения
local   all             all                                     trust

# Подключение по сети (в Docker)
host    all             all             0.0.0.0/0               md5
