
Структура папки esp32/
```
esp32/
├── common/
│   └── README.md           # Общие инструкции и рекомендации для всех ESP32 модулей
│
├── esp32_module_01/
│   ├── README.md           # Локальная инструкция по работе с этим модулем
│   ├── config.h            # Настройки модуля (GPIO, I2C адреса, параметры сенсоров)
│   └── esp32_module_01.ino # Основной Arduino/ESP32 скетч
│
└── README.md               # Главная инструкция по папке esp32
```

🔹 Примечания по каждому файлу

* common/README.md – содержит общие рекомендации для всех ESP32 модулей: структура кода, naming convention, подключение к MQTT, обработка сенсоров, версия прошивки и т.д.

* esp32_module_01/README.md – локальная инструкция для конкретного модуля:

  * как прошивать .ino,

  * как подключать сенсоры,

  * какие GPIO и I²C адреса используются,

  * куда отправляются данные по MQTT.

* config.h – конфигурационный файл модуля:

  * определяет пины сенсоров, I²C адреса, параметры частоты опроса датчиков, порты MQTT.

  ```* Пример содержимого:
    
    #define DEVICE_ID "esp32_module_01"
    #define CO2_VOC_PIN_SDA 21
    #define CO2_VOC_PIN_SCL 22
    #define PHOTODIODE_PIN_SDA 21
    #define PHOTODIODE_PIN_SCL 22
    #define TEMP_HUMIDITY_PIN 4
    #define SOIL_MOISTURE_AN_PIN 34
    #define SOIL_MOISTURE_DIG_PIN 23
    #define LCD_I2C_ADDRESS 0x27
    #define ULTRASONIC_TRIG_PIN 18
    #define ULTRASONIC_ECHO_PIN 25
    ```

* .ino файл – основной скетч Arduino/ESP32. Отвечает за:

  * опрос сенсоров,

  * публикацию данных на MQTT (mqtt://mosquitto:1883),

  * обработку команд с backend (через MQTT топики),

  * подключение к Wi-Fi, настройка OTA и т.д.

* README.md в корне esp32/ – общая инструкция для всех ESP32 модулей в проекте:

  * структура папки,

  * рекомендации по добавлению новых модулей,

  * использование шаблона template_esp32.json в backend,

  * как backend автоматически подхватывает новые JSON-файлы.
