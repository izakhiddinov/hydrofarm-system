# ESP32 Modules

В этой папке хранятся прошивки и конфигурации ESP32 модулей.

Каждый ESP32 имеет отдельную папку:
- один модуль = одна зона / теплица / грядка
- связь осуществляется через MQTT

Код будет добавлен позже.
