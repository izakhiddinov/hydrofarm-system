# HydroFarm Backend - Developer Guide

Этот файл предназначен для разработчиков, которые работают с Python backend для HydroFarm.

---
```
1️⃣ Структура папки backend/
backend/
├── app.py                 # Основной скрипт backend
├── mqtt_client.py         # Работа с MQTT (подписка на топики ESP32, публикация данных)
├── db.py                  # Работа с PostgreSQL
├── config.py              # Глобальные настройки проекта (константы, пути, параметры подключения)
├── gpio.py                # Управление GPIO на Raspberry Pi (реле, датчики)
├── requirements.txt       # Список зависимостей Python
├── README.md              # Локальная инструкция для backend-разработчиков
├── Dockerfile             # Dockerfile для сборки backend-контейнера
└── configs/
    └── template_esp32.json  # Шаблон JSON для добавления новых ESP32 и сенсоров
```

🔹 Примечания по каждому файлу

* app.py – основной скрипт backend. Считывает данные с ESP32 через MQTT, обрабатывает и сохраняет в PostgreSQL.

* mqtt_client.py – подписка на топики ESP32, публикация данных в MQTT (для тестов или отсылки на другие топики).

* db.py – подключение к PostgreSQL, функции записи и чтения данных.

* config.py – хранит параметры подключения к MQTT, PostgreSQL, константы GPIO и I²C адресов.

* gpio.py – обертка для работы с реле, насосами и другими GPIO Raspberry Pi.

* requirements.txt – например:
```
paho-mqtt
psycopg2-binary
RPi.GPIO
python-dotenv
```

* README.md – локальная инструкция для backend-разработчиков. Содержит шаги запуска, добавления новых ESP32 и сенсоров через template_esp32.json.

* Dockerfile – сборка backend-контейнера, базируется на Python, устанавливает зависимости из requirements.txt.

* configs/template_esp32.json – шаблон JSON для новых ESP32 модулей. Пример содержимого:
```
{
  "device_id": "esp32_module_01",
  "sensors": [
    {
      "type": "CO2/VOC_1",
      "pin": "I2C",
      "i2c_address": "0x5A"
    },
    {
      "type": "illumination_1",
      "pin": "I2C",
      "i2c_address": "0x23"
    }
  ]
}
```
🔹 Примечание: Для каждого нового ESP32 модуля создаётся отдельный JSON-файл на основе template_esp32.json с уникальным device_id, GPIO и I²C адресами. Backend автоматически подхватывает новые файлы, поэтому программисту не нужно менять Python код.


