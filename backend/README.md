📁 backend/README.md

```
# HydroFarm Backend

Backend — это центральный мозг системы HydroFarm.  
Он принимает данные от ESP32 по MQTT, работает с GPIO Raspberry Pi, сохраняет данные в PostgreSQL и предоставляет API для веб-интерфейса.

---

## 📂 Структура папки `backend/`

```text
backend/
├── app.py                 # Точка входа backend-приложения
├── mqtt_client.py         # Подключение к MQTT (ESP32 → backend)
├── db.py                  # Работа с PostgreSQL
├── config.py              # Общие настройки проекта
├── gpio.py                # Управление GPIO Raspberry Pi
├── requirements.txt       # Python-зависимости
├── Dockerfile             # Docker-образ backend
├── README.md              # Этот файл
└── configs/
    └── template_esp32.json  # Шаблон конфигурации ESP32
```

🚀 Назначение backend

Backend выполняет следующие задачи:

* Подключается к MQTT-брокеру (Mosquitto)

* Принимает данные от ESP32

* Управляет насосами, клапанами и светом через GPIO Raspberry Pi

* Сохраняет данные сенсоров в PostgreSQL

* Предоставляет API для web-интерфейса

* Позволяет добавлять новые ESP32 и датчики без изменения кода

🔌 MQTT
Подключение

Backend подключается к MQTT по следующим параметрам:

* Broker: mosquitto

* Port: 1883

* Пример топиков:

    * hydrofarm/esp32/<device_id>/sensors

    * hydrofarm/commands/<device_id>

Все параметры берутся из переменных окружения (Docker).

🧩 template_esp32.json — ключ к масштабируемости

Файл configs/template_esp32.json — это шаблон описания одного ESP32.

📌 ВАЖНО

Это не код и не прошивка, а конфигурация, которую:

* в будущем может создавать админ через браузер

* backend может хранить в БД

* backend использует для понимания, какие датчики есть у устройства

Пример:
```
{
  "device_id": "esp32_module_01",
  "location": "Greenhouse A",
  "sensors": [
    {
      "name": "co2_sensor",
      "type": "I2C",
      "model": "CCS811",
      "i2c_address": "0x5A"
    },
    {
      "name": "air_temp_humidity",
      "type": "DIGITAL",
      "model": "DHT22",
      "gpio": 4
    }
  ]
}
```

Как добавляется новый ESP32 (логика)

1. Создаётся новая конфигурация (JSON или запись в БД)

2. Backend автоматически:

    * принимает данные

    * сохраняет их

    * отображает в web-интерфейсе

❗ Код backend при этом не меняется

🐍 Python-зависимости

Устанавливаются из requirements.txt:
```
pip install -r requirements.txt
```

Основные библиотеки:

paho-mqtt

psycopg2

flask / fastapi (если используется API)

RPi.GPIO

🧪 Локальный запуск (без Docker)
```
export MQTT_BROKER=localhost
export MQTT_PORT=1883
export POSTGRES_HOST=localhost
export POSTGRES_DB=hydrofarm
export POSTGRES_USER=hydrofarm_user
export POSTGRES_PASSWORD=hydrofarm_pass

python app.py
```

🧠 Важно помнить

* Backend не знает заранее, сколько ESP32 будет

* Все устройства описываются конфигурациями

* Добавление новых датчиков не требует программиста

* Backend должен быть максимально универсальным

📌 Рекомендации разработчикам

* Не хардкодить GPIO и датчики

* Всё новое — через конфигурации

* Любые изменения фиксировать в GitHub

* Таблицы подключений обновлять в docs/contracts/developer_contract.md
```

---

# 📁 `docker/README.md`

```md
# HydroFarm Docker

Папка `docker/` содержит всё необходимое для запуска инфраструктуры HydroFarm через Docker.

---

## 📂 Структура папки `docker/`

```text
docker/
├── docker-compose.yml     # Запуск всех сервисов
├── mosquitto/
│   ├── mosquitto.conf     # Конфигурация MQTT брокера
│   └── README.md
└── postgres/
    ├── init.sql           # Инициализация БД
    ├── postgresql.conf    # Конфигурация PostgreSQL (НЕ подключается напрямую)
    ├── pg_hba.conf        # Политика доступа (НЕ подключается напрямую)
    └── README.md
```

```
🧱 Сервисы Docker
Сервис	Имя контейнера	Хост внутри Docker	Порт
Mosquitto	hydrofarm-mosquitto	mosquitto	1883
PostgreSQL	hydrofarm-postgres	postgres	5432
Backend	hydrofarm-backend	backend	8000
```




