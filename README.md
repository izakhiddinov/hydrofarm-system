## 🧠 ЛОГИКА СИСТЕМЫ (Для новичков)

Система работает как «Почта»:
1. **Талгат (Frontend)** пишет письмо: "Включи Насос 1" и отправляет его в API.
2. **Бэкенд** кладет это письмо в ящик (База Данных).
3. **Агент (Raspberry Pi)** каждые 2 секунды заглядывает в ящик, видит письмо и дергает контакт (GPIO).

### 📝 Команды для Талгата (Frontend API)

Чтобы управлять системой из JavaScript (index.html), используй эти адреса:

* **Получить все данные:** `GET http://localhost:8000/api/data`
* **Добавить/Настроить устройство:** `POST http://localhost:8000/api/devices/setup`
    * *JSON тело:* `{"id": "pump_1", "pin_number": 17, "connection_type": "relay"}`
* **Включить/Выключить:** `POST http://localhost:8000/api/command?device_id=pump_1&command=PUMP_ON`

### ⚡ Электрическая логика (Active LOW)
**ВАЖНО:** Мы используем реле, которые включаются «минусом».
- Команда **ON** = Сигнал `LOW` (0 вольт).
- Команда **OFF** = Сигнал `HIGH` (3.3 вольта).